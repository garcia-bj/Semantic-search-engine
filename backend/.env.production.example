# ============================================
# CONFIGURACIÓN DE PRODUCCIÓN
# ============================================
# Copia este archivo como .env.production y configura los valores

# Entorno
NODE_ENV=production
PORT=3001

# Base de datos PostgreSQL
# Para Docker: usa el nombre del servicio (postgres)
# Para servidor externo: usa la IP/dominio del servidor
DATABASE_URL=postgresql://semantic_user:semantic_password@postgres:5432/semantic_search

# Apache Jena Fuseki (Triple Store)
FUSEKI_URL=http://fuseki:3030
FUSEKI_DATASET=semantic
FUSEKI_USERNAME=admin
FUSEKI_PASSWORD=admin123

# Elasticsearch
ELASTICSEARCH_NODE=http://elasticsearch:9200

# Cloudinary (opcional - para upload de archivos)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# CORS (dominios permitidos, separados por coma)
CORS_ORIGINS=http://localhost:3000,https://tu-dominio.com

# Logs
LOG_LEVEL=info

# ============================================
# NOTAS IMPORTANTES
# ============================================
# 1. Si usas Docker Compose, los nombres de host deben ser los nombres de los servicios
#    Ejemplo: postgres, fuseki, elasticsearch
#
# 2. Si despliegas en servicios separados, usa las URLs completas
#    Ejemplo: DATABASE_URL=postgresql://user:pass@db.ejemplo.com:5432/semantic_search
#
# 3. NUNCA subas este archivo con valores reales a Git
#
# 4. Para Railway/Render/Heroku, configura estas variables en el dashboard
